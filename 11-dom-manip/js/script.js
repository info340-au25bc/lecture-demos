const clickHandler = () => {
  console.log("I am here!!");
};

// let dog = document.querySelector('#dog');
// dog.addEventListener("click", clickHandler);

// let cat1 = document.querySelector('#cat1');
// cat1.addEventListener("click", clickHandler);

function renderText  () {
  // given a string, adds a list item to
  // a list on the page.
  let p = document.querySelector('text');
  p.appendChild(...)
}

// Given a button, and a text field,
// add the text field's value to a list
let input = document.querySelector('#text_input');
input.value
